import ProfileForm from "../LoginComponents/ProfileForm";
import NavigationBar from "../LoginComponents/NavigationBar";

const ProfilePage = () => {
  return(
    <div>
    <NavigationBar/>
    <ProfileForm />
    </div>
  )
};

export default ProfilePage;
